Given a subfolder of browser_traces (ex. 'browser_traces/amazon_single_item_purchase_30_p004/run_001'), your goal should be to try and find "macros" that we can give our browser-use model in order to compress the number of inference calls it has to make for a given task. For example, if the browser-use model is trying to complete a browser-use-based task, like purchasing a single item on Amazon, and it recognizes it should run a macro (from a list of pre-defined macros), using slot-filling to put in the correct parameters; then, it can technically save a few inference calls because it is able to run the macro without having to look at the screen for those intermediate steps. To actually mine the macros, I think the best approach as of right now would be to maybe try something along the lines of BPE tokenizing (can think of other methods later), where we observe the list of "model_actions" located in the trace JSON at "result" -> "browser_use" -> "model_actions," and run a BPE tokenizer on it. Your job is to create a script called bpe_macro_tokenizer.py that finds meaningful macros and constructs them into an API-like list that we could append to every browser-use inference call (ideally, the macro should have a name, description, defined parameters, and the "code"-block that would be used to execute the macro—these can be generated using an LLM [preferably a Claude model]). In theory, we should have a trade-off of using more tokens per query but reducing the number of calls (since some macros should be able to prevent intermediate steps from being done over and over again across several tasks). The resulting JSON of macros should be saved to a subfolder of 'macros' (so, using the previous example, 'macros/amazon_single_item_purchase_30_p004_run_001/bpe_tokenizer_macros.json'). Moreover, include any plots/visuals that you think would be helpful in identifying the effectiveness of the macros identified (put this in the same subfolder as the JSON, so using the previous example, 'macros/amazon_single_item_purchase_30_p004_run_001/'). Don't worry about actually applying the macros we find as of yet. We can do this later. Ask any questions you may have before implementing this script. You should only have to modify the new script we're creating, bpe_macro_tokenizer.py.